show exact carrier fee name in order details(like in settings) with per carrier lingo
explain what fees and such are

on order details show the duty percent per HS code in order details
say if they are using default HS code or not

more zonos coloring in Dashboard

show exact currency conversion rate for

buffer per hs code
buffer per country
   buffer per country but also with just a specific HS code

is it compliant to add buffers on duty rates?

on order details, if an item is using a default code instead of per item, yell at them


ability to add item to order and recalculate (and remove) and then still do LCG
