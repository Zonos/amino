name: Publish Package
# https://docs.npmjs.com/trusted-publishers
on:
  push:
    branches:
      - main
      - feat/migrate-to-shadcn

permissions:
  id-token: write # Required for OIDC
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v5
      - uses: actions/setup-node@v6
        with:
          # 22.22.2 ships with a broken bundled npm (missing promise-retry)
          node-version: 22.22.1
          registry-url: "https://registry.npmjs.org"
          cache: pnpm
      # Ensure npm 11.5.1 or later for trusted publishing
      - run: npm install -g npm@latest
      - run: pnpm install
      - run: pnpm build:ci
      # Publish with "alpha" dist-tag for non-main branches, "latest" for main
      - run: cd dist && npm publish --access public --tag ${{ github.ref_name == 'main' && 'latest' || 'alpha' }}
